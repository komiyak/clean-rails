Install the specific version of Rails.

```
cd clean6_0/
bundle --path .
```

And then, create the rails app without bundle installation.

```
bundle exec rails new rails_app --skip-bundle --skip-git
```
